# Curated set (WU3.5 → WU8.5). Read by scripts/seed_curated_set.py.
#
# WU8.5 expansion. The full 31-fixture inventory under data/samples/ is
# now seeded. Each demo subscription (UK/BANKING and EU/BANKING) sees
# >=10 documents via per-doc ``jurisdiction:`` relabels. Fixtures with
# no demo relabel keep their original ISO-2 jurisdiction and are
# visible to admin only.
#
# Token convention: jurisdictions are ISO-2 (matching ``fixtures.json``)
# plus the synthetic relabels UK and EU used by the demo accounts.
# Per-doc ``jurisdiction:`` overrides relabel a captured fixture onto a
# demo token without forking the upstream capture. Sectors use the
# canonical taxonomy documented in
# ``packages/horizons-core/src/horizons_core/db/schema.md`` (BANKING,
# INSURANCE, employment, tax, consumer-protection, corporate-
# governance, …). The WU8.1 demo accounts subscribe to BANKING only,
# so every relabel below sets ``sector: BANKING`` regardless of the
# fixture's editorial origin — the relabel cluster makes the demo
# subscriptions visibly populated rather than honest about source
# taxonomy. Honest-sector docs (no relabel) stay on their native sector
# so admin can still see the taxonomy mix.

jurisdictions: [IE, GB, UK, EU, BE, AT, DE, FR, IT, ES, DK, FI, GR, AU, FJ, AD, AE, CY, GE, BR, CN, CH, CZ, HR, HU, JP, KR, LU, LV, MC, AL]
# Note: BE/AT/ES/DK/FI/GR/FJ/AD/AE/CY/GE/BR/CN are listed for the still-on-disk
# fixtures referenced in journal/docs, but after WU8.7 none of them are seeded
# (no demo account subscribes to them and the relabel cluster moved to native
# GB/EU sources). The fixture files stay on disk for historical reference.
sectors:
  - BANKING
  - employment
  - tax
  - consumer-protection
  - corporate-governance
default_cadence_hours: 24

documents:
  # =====================================================================
  # The "always changing" demo signal. Short cadence so the claim loop
  # touches it every demo session. Native IE/corporate-governance —
  # neither demo client sees this; admin does.
  # =====================================================================
  - id: "8064194"           # IE — CRO Social Media Policy
    sector: corporate-governance
    cadence_hours: 1

  # =====================================================================
  # UK relabels (10) — visible to demo-uk@demo.example.com
  # (subscription = UK / BANKING). WU8.7 pivot: every entry below is now
  # natively GB-sourced from a banking-/finance-/legal-relevant UK
  # portal. The 3 v2-attached docs (28914588, 27732019, 2145602) still
  # carry their synthetic v2 demo signals; 27732019 (IE) and 2145602
  # (AU) keep the UK relabel because their v2 edits exercise MOVED +
  # ADD/REMOVE change-event types. Every other UK entry is a fresh
  # capture from a UK regulator / court / parliament portal — no more
  # Chinese / Catalan / Brazilian fillers.
  # =====================================================================

  # GB Foat v DWP — work-and-pensions tribunal judgment. Already the
  # WU8.1 headline diff: the UK demo client opens this for the diff
  # beat. Synthetic v2 staged under data/samples/synthetic_v2/.
  - id: "28914588"
    jurisdiction: UK
    sector: BANKING

  # IE — Irish Statute Book Act; the dense PART/Section/(a)/(i) fixture.
  # Relabelled to UK so it shows up in the UK list as a long, well-
  # structured doc to demo the clause-overlay toggle on.
  # Synthetic v2 carries the demo's only MOVED change event (section 11
  # renumbered to 11A) plus one MODIFIED edit (Minister's order-making
  # power upper bound widened from 12 to 16 weeks).
  - id: "27732019"
    jurisdiction: UK
    sector: BANKING

  # AU — Synthetic v2 staged with ADDED (new Schedule 1 closing
  # sentence pinning the LGA boundary reference date) + REMOVED (sewage-
  # contamination limb of the major damage definition) edits.
  - id: "2145602"
    jurisdiction: UK
    sector: BANKING

  # GB FCA — Handbook corrections (74 KB). Banking-rulebook flavour.
  - id: "32416312"
    jurisdiction: UK
    sector: BANKING

  # GB Bank of England — External business of Monetary Financial
  # Institutions report (485 KB). Banking-supervision flavour.
  - id: "37048477"
    jurisdiction: UK
    sector: BANKING

  # GB Payment Systems Regulator — early speech "A new regulator for a
  # new world". Payments-policy flavour.
  - id: "2136076"
    jurisdiction: UK
    sector: BANKING

  # GB Legislation — Tobacco and Vapes Act 2026 (551 KB). Primary
  # legislation; numbered Parts/sections; good clause-overlay fixture.
  - id: "35840254"
    jurisdiction: UK
    sector: BANKING

  # GB Parliament — "What's on in the Lords 8-11 June" briefing.
  # Lighter content, kept for jurisdictional spread.
  - id: "37512048"
    jurisdiction: UK
    sector: BANKING

  # GB Competition Appeals Tribunal — Roadget v Shein (162 KB). Markets/
  # competition case.
  - id: "36992553"
    jurisdiction: UK
    sector: BANKING

  # GB Bailii — Affirmative Finance Limited v Pearson (38 KB). Consumer-
  # credit-adjacent case-law fixture.
  - id: "9787341"
    jurisdiction: UK
    sector: BANKING

  # =====================================================================
  # EU relabels (10) — visible to demo-eu@demo.example.com
  # (subscription = EU / BANKING). WU8.7 pivot: the 4 v2-attached docs
  # (31366184 BEREC, 31702142 FR ACPR, 20951816 DE BA, 26863 IT MEF)
  # carry the demo's diff signals and keep their existing labels; FR/
  # DE/IT are EU-member-state regulators relabelled to EU. The other 6
  # entries are fresh captures from native EU banking institutions
  # (EBA, ECB, ECB Banking Supervision, SRB, ESMA, EIOPA) — no more
  # national-only fillers.
  # =====================================================================

  # EU — BEREC press item, native EU jurisdiction.
  # Synthetic v2 staged with MODIFIED (debriefing date postponed from
  # 10 to 17 June 2026) + REMOVED (closing paragraph on livestream/Q&A)
  # edits.
  - id: "31366184"
    sector: BANKING

  # FR — ACPR prudential decision. Relabelled to EU; the synthetic v2
  # for this id is the EU demo client's headline diff beat.
  - id: "31702142"
    jurisdiction: EU
    sector: BANKING

  # DE — Bundesagentur für Arbeit labour-market report. Synthetic v2
  # staged with ADD/REMOVE/MODIFIED edits.
  - id: "20951816"
    jurisdiction: EU
    sector: BANKING

  # IT — MEF economic/financial document. Synthetic v2 staged with
  # ADD/REMOVE/MODIFIED edits (the headline 2017 net-borrowing target
  # revision).
  - id: "26863"
    jurisdiction: EU
    sector: BANKING

  # EU European Banking Authority — Report on benchmarking of
  # diversity practices in the EU banking sector (107 KB).
  - id: "28943439"
    sector: BANKING

  # EU European Central Bank — Fiscal policy transmission research
  # paper (199 KB).
  - id: "34656519"
    sector: BANKING

  # EU ECB Banking Supervision — "Navigating risk, cutting complexity"
  # speech on financial conglomerates (39 KB).
  - id: "37071316"
    sector: BANKING

  # EU Single Resolution Board — Speech "From resolution planning to
  # operational readiness".
  - id: "21435108"
    sector: BANKING

  # EU European Securities and Markets Authority — Report on quality
  # and use of data 2025 (190 KB).
  - id: "31915825"
    sector: BANKING

  # EU European Insurance and Occupational Pensions Authority — Final
  # Report on revised Guidelines (85 KB).
  - id: "28263756"
    sector: BANKING

  # =====================================================================
  # Admin-only — no demo client matches. Kept honest to source
  # jurisdiction + sector. Visible to admin-demo@demo.example.com only.
  # Adds taxonomy / language / size diversity to the admin browser.
  # =====================================================================

  - id: "20950489"          # CH
  - id: "29662776"          # CZ
  - id: "6339302"           # HR
  - id: "9119685"           # HU
  - id: "1771371"           # JP
  - id: "5412226"           # KR
  - id: "5444178"           # LU
  - id: "34988027"          # LV
  - id: "7574537"           # MC
  - id: "31592917"          # AL
