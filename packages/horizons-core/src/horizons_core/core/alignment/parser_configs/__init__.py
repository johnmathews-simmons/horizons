"""Bundled per-portal :class:`ParserConfig` YAML files.

Loaded via :mod:`importlib.resources` by
:mod:`horizons_core.core.alignment.portal_config`. Adding a portal is
a YAML file in this directory plus a test landmark — no code change.
"""
