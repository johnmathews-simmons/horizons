"""Horizons core: shared domain types, schema models, repository bases, Lawstronaut client.

Application code lands in later work units. This package currently exposes only
the version marker so the workspace and test harness can import it.
"""

__version__ = "0.0.0"
