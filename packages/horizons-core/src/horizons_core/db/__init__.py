"""Database layer: engines, sessions, repositories, role/RLS infrastructure.

The schema itself lives in ``packages/horizons-core/migrations/``; this
package owns the runtime side (connections, session-scoped GUCs,
repository helpers).
"""
